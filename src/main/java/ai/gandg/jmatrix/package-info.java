
/**
 * A vector/matrix library which optimizes performance through polymorphism
 * and allows caching the (final/intermediate) results to reduce the overhead
 * of garbage collection.
 */
package ai.gandg.jmatrix;
