Refactoring:

Workflow warning in README.
Proper exception handling.

-------------------------------------------------------------------------------
Functionality:

vector norms
horzcat, vertcat, dot product, pow

bsxfun (Binary Singleton Expansion Function)
(PLUS, MINUS, TIMES, DIVIDE, MOD, MIN, MAX}

singular value decomposition
back substitution
least squares solution
spectral norm computation

sparse vectors/matrices

-------------------------------------------------------------------------------
Performance:

Benchmarking:
  - matrix multiplication

diagonal matrices, identity matrix
lower/upper triangular matrices

-------------------------------------------------------------------------------
Ideas:

Column major order for Matrix representation?
create(double[][]) looks row major though

Instead of array, store the representation (DenseMatrix, TDenseMatrix)
inside the Matrix class and implement in-place transpose, etc...
